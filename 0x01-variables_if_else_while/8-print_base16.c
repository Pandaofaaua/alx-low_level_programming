#include <stdio.h>
/**
 * main - Printing hexadecimal
 *
 * Return: 0
 */
int main(void)
{
	int i;
	int g;

	i = 0;
	g = 'a';
	while
		(i < 10) {
			putchar(i + '0');
			i++;
		}
	while
		(g < 'g') {
			putchar(g);
			g++;
		}
	putchar('\n');
	return (0);
}
